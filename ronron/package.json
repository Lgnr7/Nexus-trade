{
  "name": "ronron",
  "version": "1.0.0",
  "private": true,
  "description": "Ronron — boutique en ligne d'accessoires pour chats (dropshipping, paiement Stripe).",
  "type": "module",
  "engines": {
    "node": ">=20 <25"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node scripts/check.js",
    "test": "node --test \"test/**/*.test.js\""
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "stripe": "^22.6.2"
  }
}
